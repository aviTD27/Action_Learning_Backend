package fr.epita.enums;

/** What a student is expected to submit for an assignment. */
public enum SubmissionType {
    FILE,   // file upload only
    TEXT,   // inline text only
    BOTH    // either / both
}
